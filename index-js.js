import React from 'react';
   import ReactDOM from 'react-dom';
   import ShiftManagementSystem from './shift-management-system';

   ReactDOM.render(
     <React.StrictMode>
       <ShiftManagementSystem />
     </React.StrictMode>,
     document.getElementById('root')
   );
   